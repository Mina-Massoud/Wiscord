/**
 * @deprecated Use `ServerWorkspacePage` — kept so stale route bundles that still
 * lazy-load this path render the real server workspace instead of the old card.
 */
export { default } from './ServerWorkspacePage';
