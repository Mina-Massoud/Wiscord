export function EmptyPreview(): React.JSX.Element {
  return (
    <div className="text-ink-muted text-caption flex flex-1 flex-col items-center justify-center text-center">
      Pick a question on the left to preview it.
    </div>
  );
}
